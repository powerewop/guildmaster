import java.util.Random;

/**
 * Created by Gleb on 17.11.2016.
 */
public class MapGenerator {


    public static void main(String[] args) {

        buildMap();
    }

    private static void buildMap() {
        /*int mapSize = 10000;
        Random random = new Random();
        int maxX = mapSize / (random.nextInt(3) + 2);
        int maxY = mapSize - maxX;

        System.out.println(maxX);

        Geksagon[] map = new Geksagon[mapSize];

        int geksNumber = 0;
        for (int x = 0; x < maxX; x++) {
            for (int y = 0; y < maxY; y++) {
                map[geksNumber] = new Geksagon(x, y, geksNumber);
                y++;
                geksNumber++;
            }
            x++;
        }
        System.out.println(map[7].toString() + ", индекс ячейки: " + map[7].getIndex());
        System.out.println("Размер карты: " + mapSize/2 +"x" + mapSize/2);
        */
    }

    private static void buildTown(){

    }
}
